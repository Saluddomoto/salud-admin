# apps/admin — 社内管理システム

Salud 基幹システムの社内向けアプリ（MVP）。

## セットアップ

```bash
# ルートから
pnpm install
cp ../../.env.example .env.local
# .env.local に Firebase の設定値を記入

pnpm dev
```

## 開発

`http://localhost:3000` で起動。

Phase 2 で Next.js プロジェクトを作成します。
